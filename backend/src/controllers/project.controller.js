export const createProject = async (req, res) => {
  res.json({ success: true, message: "createProject placeholder" });
};

export const listProjects = async (req, res) => {
  res.json({ success: true, message: "listProjects placeholder" });
};

export const updateProject = async (req, res) => {
  res.json({ success: true, message: "updateProject placeholder" });
};

export const deleteProject = async (req, res) => {
  res.json({ success: true, message: "deleteProject placeholder" });
};
