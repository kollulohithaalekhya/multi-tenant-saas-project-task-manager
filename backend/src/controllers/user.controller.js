// User controller (ES Module syntax)
// Temporary placeholders to prevent crashes

export const addUser = async (req, res) => {
  res.json({ success: true, message: "addUser placeholder" });
};

export const listUsers = async (req, res) => {
  res.json({ success: true, message: "listUsers placeholder" });
};

export const updateUser = async (req, res) => {
  res.json({ success: true, message: "updateUser placeholder" });
};

export const deleteUser = async (req, res) => {
  res.json({ success: true, message: "deleteUser placeholder" });
};
