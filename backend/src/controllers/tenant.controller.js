// Temporary placeholders to prevent crash
// Real logic will be added later

export const getTenantDetails = async (req, res) => {
  res.json({ success: true, message: "getTenantDetails placeholder" });
};

export const updateTenant = async (req, res) => {
  res.json({ success: true, message: "updateTenant placeholder" });
};

export const listTenants = async (req, res) => {
  res.json({ success: true, message: "listTenants placeholder" });
};
